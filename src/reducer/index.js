const inizialstate = {
  players: [],
  player: {},
};

function rootReducer(state = inizialstate, { type, payload }) {
  switch (type) {
    
    default:
      return state;
  }
}
export default rootReducer;
